server:
  port: 1234

database:
  engine: mysql
  mysql:
    port: 3306
    user: root
    passwd: 1234
    endpoint: 127.0.0.1
    database: 10n1s
    maxIdleConnections: 100
    maxOpenConnections: 100
    connMaxIdleTime: 5
    connMaxLifeTime: 5
    queryLogMode: true

cache:
  engine: 
    redis:
      redisAddr:
      password:
      minIdleConns:
      poolSize:
      poolTimeout:
      db:      

room:
  database:
    dbInit: true